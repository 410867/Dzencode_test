import React from 'react';

const Home = () => {
    const body = document.querySelector('body');
    body.style.background = "#f0f3f5";

    return (
        <main></main>
    );
};

export default Home;